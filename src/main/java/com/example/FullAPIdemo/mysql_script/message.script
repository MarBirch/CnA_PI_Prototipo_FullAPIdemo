CREATE TABLE message (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    role VARCHAR(255),
    content TEXT,
    chat_id BIGINT,

    CONSTRAINT fk_message_chat
        FOREIGN KEY (chat_id)
        REFERENCES chat(id)
        ON DELETE CASCADE
);